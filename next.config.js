/** @type {import('next').NextConfig} */
const nextConfig = {
    //output: 'export', // For Next.js 13+, use 'export'
    // or if using older versions:
    // trailingSlash: true,
    images: {
        domains: ['durgapujo.in'],
    },
};

module.exports = nextConfig;
